# Changelog

## 0.9.9 - 2026-09-15

社区插件目录提交前的规范化，并合并远程并行的 0.8.x 开发线。

- **命令 ID 去掉冗余前缀**：Obsidian 会自动为命令 ID 加上插件 ID 前缀，官方要求命令 ID
  中不要再包含插件 ID。`open-workbuddy` / `new-workbuddy-chat` / `check-workbuddy-updates`
  改为 `open` / `new-chat` / `check-updates`，注册后为 `workbuddy-ai:open` 等。
  ⚠️ 若你之前给这些命令绑过快捷键，需要在「设置 → 快捷键」里重新绑定。
- **合并远程 0.8.x 线**：远程 `main` 上另有一条 0.8.x 开发线（社区商店 ID/名称合规改名、
  CSS lint 修复、README 标题、`showToolCalls` 开关等 20 个提交）。逐项核对确认其改动
  已全部包含在 0.9.x 中（`!important` 0 处、`setHeading()` 3 处、`border-bottom` 替代
  `text-decoration`、ID `workbuddy-ai` / 名称 `WorkBuddy AI` 均一致），故以本地 0.9.x 为准
  合并，远程提交与 0.8.7/0.8.8/0.8.9 标签保留在历史中。
- 提交前合规复核：`innerHTML` 0 处、`vault.modify` 0 处、示例代码无残留、
  `isDesktopOnly: true`、`description` 146 字符且句号结尾、无 `fundingUrl`。

# Changelog

## 0.9.8 - 2026-09-15

思考过程流式输出时，页面自动跟着滚到最新生成的那一行。

- **根因**：`appendThought()` 追加文字后没有再滚一次（`agent-text` 分支有、
  `thought` 分支漏了），所以思考阶段页面纹丝不动。
- 但**只加一句「滚到底」是不够的**：`.workbuddy-thought pre` 有
  `max-height: 180px; overflow: auto`，思考内容超过 180px 后外层容器就不再长高，
  新增文字全部落进 pre 的内部滚动区。实测（无头 Chromium，24 轮注入）：
  pre 可视高 180px / 内容高 594px，外层容器在第 8 轮后停止增长，之后的 560 个字符
  都在 pre 内部 —— 此刻只滚外层容器仍有 **414px 内容看不见**，同时滚 pre 则为 0。
  所以现在追加文字后**先滚 pre 自身、再滚外层容器**。
- `scrollToBottom()` 改为**每帧合并**：思考 delta 一帧可能来好几个，逐个触发
  平滑滚动会互相打断，最终位置反而落后于最新内容。同一帧内只滚一次。
- 折叠态（`open === false`）直接跳过滚动 —— 新内容不可见，跟着滚只会把回答顶出视野。
- 用户正在消息区拖选时跳过，沿用 0.9.1 的选区保护，不抢选区与滚动位置。

# Changelog

## 0.9.7 - 2026-09-15

「思考过程」默认展开，不用每次手点了。

- **根因**：`appendThought()` 创建 `<details class="workbuddy-thought">` 时没设 `open` 属性，
  而原生 `<details>` 的默认态就是折叠。
- 创建时按 `settings.thoughtExpanded`（默认 `true`）决定是否展开。**只在创建时设置一次** ——
  该元素在整轮思考中全程复用、不重建，所以你手动折叠后不会被下一个流式 delta 重新顶开。
- 设置面板新增「思考过程默认展开」，随时可改回折叠。`SETTINGS_VERSION` 4 → 5，
  老配置走 migration 补默认值。
- 长思考不会挤占回答：`.workbuddy-thought pre` 本来就有 `max-height: 180px; overflow: auto`，
  展开后在块内滚动。
- 类型修正：`querySelector<HTMLElement>` 推断不出 `open` 属性，改为 `HTMLDetailsElement`。
- 顺手修：`workbuddy-client.ts` 里的 `PLUGIN_VERSION` 硬编码停在 `"0.9.0"`，从 0.9.0 之后
  就没同步过，会把错的插件版本通过 `initialize` 报给 CLI。改为由 `main.ts` 传入
  `this.manifest.version`。

## 0.9.6 - 2026-09-15

让「思考过程」用简体中文输出（可在设置里关掉）。

- **根因**：思考过程（thinking / reasoning）是模型在内部生成的文本，插件只负责渲染，
  UI 层没法翻译或改写。多数通用模型默认走英文推理 —— 读取英文文件名、代码、命令输出
  时尤其明显。`codebuddy --help` 已确认 ACP 启动参数里没有任何语言开关，所以唯一可行
  的手段是每轮在 prompt 里显式约束。
- 新增 `src/core/thought-language.ts`，`buildThoughtLanguageInstruction()` 生成中文推理
  约束，措辞有三个刻意设计：① 同时点名 "reasoning" 与 "thinking" 两个通道名（不同模型
  叫法不同）；② 显式覆盖「即使资料是英文也保持中文推理」这个最常见的破功场景；③ 声明
  代码、命令、文件路径、专有名词不翻译，避免模型为了输出全中文而翻译路径导致找不到文件。
- 约束拼在每轮 prompt 的最前面（常驻指令之前），保证优先级最高。
- 设置面板新增「思考过程语言」：简体中文（默认）/ 跟随模型默认。选后者时返回空串、
  不拼接，不产生额外 token。
- `SETTINGS_VERSION` 3 → 4，老配置走 migration 补默认值，不用手动改。
- 新增 `tests/thought-language.test.ts`（9 例）。

> 说明：这是**提示词层面的约束**，不是硬开关。绝大多数模型会遵守；若某个模型仍坚持
> 英文推理，可换用对中文推理更友好的模型，或在设置里改为「跟随模型默认」。

## 0.9.5 - 2026-09-15

修复模型下拉框里长模型名被裁掉（`Deepseek-V4.1-Flash` 显示成 `Deepseek-V4.1-Fla`）。

- **根因**：`.workbuddy-model-select-input` 写死 `width: 112px`（当初是为了「加载模型列表前后尺寸一致」），
  模型名只要超过这个宽度就被浏览器裁掉，右侧的 `@ 资料` / `关联资料` 也无从让位。
- **改法**：宽度改为按「当前选中项」的文字宽度动态计算 —— canvas `measureText` 量文本，
  字体取 select 的实际计算样式，再加上左右内边距与下拉箭头的固定开销，限幅 **112–240px**。
  既能让长名字完整显示并顺势把右边按钮推开，又不会像「按最长 option 自适应」
  那样被列表里的超长名字撑爆。计算逻辑抽到 `src/ui/model-select-width.ts` 的纯函数
  `computeModelSelectWidth()`，方便单测。
- **工具栏补 `flex-wrap: wrap`** 并禁止子元素收缩：宽度不够时整行换行，而不是把按钮文字挤变形。
- select 的 `title` 现在显示当前模型名，悬停即可看全（即便触及 240px 上限被裁）。
- 新增 `tests/model-select-width.test.ts`（5 例）。

## 0.9.4 - 2026-09-15

把回答正文改成更紧凑、更纤细的「互联网产品」观感。

- **正文终于有了自己的排版声明**：此前 `.workbuddy-message-body` 只写了
  `line-height`，字体与字号完全继承 Obsidian 界面字体，在侧边栏里显得又大又粗
  （主题把正文字体设成衬线体时尤其明显，截图里那种"论文感"就是这么来的）。
  现在改为显式声明：`--wb-body-size` / `--wb-body-weight` / `--wb-body-font`。
- **默认值**：13px / 400 / 无衬线栈（`SF Pro Text`→`Inter`→`PingFang SC`），
  并在 macOS 上开启 `-webkit-font-smoothing: antialiased`，字形明显更纤细。
- **正文内部元素降级适配**：标题 1.18em 封顶（靠字重与颜色区分层级，不靠放大字号）、
  行内代码与代码块 0.88em 且改用等宽栈、表格 0.92em、列表与引用收紧间距、
  链接去掉下划线改虚线式底边。
- **设置面板新增两项**：「回答正文字号」（11–16px，默认 13px）、
  「回答正文字体」（无衬线 / 跟随 Obsidian 主题），改完立即生效，无需重载。
- 新增 `tests/display-settings.test.ts`：覆盖字号钳制、非法输入回落与字体栈写入。

## 0.9.3 - 2026-09-15

- **彻底移除「收藏回答」功能。** 去掉消息操作栏的星标按钮（`star`）、`StoredChatMessage.favorite` 字段及反序列化、导出 md 时插入的 `> ⭐ 已收藏的重要回答` 标记、「任务历史」里的 `· N 条收藏` 计数，以及 `.is-favorite` 样式。
- `renderResponseActions()` 的 `storedMessage` 参数已无用途，一并删除并同步两处调用点，操作栏精简为：复制 / 插入 / 替换原选区 / 保存为笔记。
- 兼容性：老 `data.json` 或历史聊天记录里残留的 `favorite` 键会被静默忽略（不报错、不再回写），无需迁移；新增回归用例覆盖该行为。

## 0.9.2 - 2026-09-15

- **移除「复制选中」按钮。** 0.9.1 已让回答正文恢复原生可选中，鼠标拖选后 `Cmd/Ctrl+C` 即可复制任意片段，这个按钮属于冗余入口，去掉后操作栏更简洁。配套删除 `copySelectionInMessage()`；`selectionWithin()` / `isNodeInside()` 工具函数保留，仍供流式渲染的选区保护逻辑使用。

## 0.9.1 - 2026-09-15

- **修复：回答正文无法选中复制。** Obsidian 核心样式对 `body` 设置了 `user-select: none`，只对 `.markdown-preview-view` / `input` / `textarea` 等白名单容器单独恢复。插件自定义视图里的 `.workbuddy-message-body` 是普通 div，因此整段回答拖不动。现在显式给消息区、差异预览、更新说明、权限弹窗代码块等纯文本区域恢复 `user-select: text`，可以像正文一样拖选任意片段后 Cmd/Ctrl+C。
- 折叠标题、任务标签、操作按钮等交互控件保持 `user-select: none`，拖选时不会误带上控件文字。
- **修复：流式输出过程中选区被冲掉。** 流式渲染每收到一个 delta 就 `setText` 重写整段 DOM 并平滑滚到底，用户正在拖选时选区和滚动位置都会被抢走。现在检测到回答区内有活动选区时跳过重写与自动滚动，选区稳定，流结束后再正常渲染 Markdown。
- 「复制选中」改为识别反向拖选（anchor 在选区外、focus 在内），并回显复制字符数。

## 0.9.0 - 2026-09-09

- 思考过程字号可调：新增「思考内容字号」设置（9/10/11/12/13/14px，默认 11px），改动立即生效，样式改由 `--wb-thought-size` 变量驱动。
- 思考块行距收敛为 1.5、内边距收紧，长思考内容在侧边栏占用更少空间。
- **权限模式默认改为 `bypassPermissions`（最大权限）**，CLI 不再逐项询问读写与命令执行。
- 新增「自动批准权限请求」开关（默认开启）：即便 CLI 仍发起询问，插件也直接选择「允许」，不再弹窗。
- 新增设置迁移（settingsVersion = 2）：老配置中的 `default` 权限模式会自动升级为 `bypassPermissions` + 自动批准。
- 补全 `versions.json` 至 0.9.0。

## 0.8.9 - 2026-08-27

- 展示名统一为 WorkBuddy AI，插件 ID 保持 `workbuddy-ai`。

## 0.8.6 - 2026-08-26

- 插件 ID 更名为 `workbuddy-ai`。

## 0.8.5 - 2026-08-26

- 插件 ID 由 `workbuddy-for-obsidian` 更名为 `workbuddy`，满足社区插件命名合规。

## 0.8.4 - 2026-08-22

- 模型选择器默认显示 **Auto**，移除 “Auto（自动选择）” 括号文案；用户手动选过的模型会被记住。
- 模型下拉固定宽度（112px），不再随选项文字自适应变化而跳动。
- 模型下拉按 `lowercase value` 去重，修复合并 CLI 偶尔返回的 “hy3” 与 “Hy3” 重复条目。
- 移除模型选择器左侧多余的图标，让按钮高度与相邻 secondary 按钮自然对齐。
- 首次进入任务页若 CLI 当前值不是 auto，会静默切换到 auto；`setConfigOption` 新增 silent 参数以容忍 CLI 不支持 auto 时的不报错。
- 替换 README 截图，反映最新的 v0.8.x 界面状态。

## 0.8.3 - 2026-08-21

- 模型选择器改为打开页面即在左下角显示，不再需要先发送一条消息。
- 未获取到模型列表时显示占位状态（默认模型 / 连接中… / 连接失败），并用悬浮提示说明。
- 打开任务页时自动预热连接 WorkBuddy，尽早拉取可用模型列表；失败静默，发送消息时自动重试。

## 0.8.2 - 2026-08-21

- 人设（常驻指令）入口移至右上角设置按钮的二级菜单，移除 composer 中的人设按钮；# 快捷编辑保留。
- 模型选择器顶部新增“Auto（自动选择）”选项，与 WorkBuddy 的 auto 模式对齐。
- 移除“资料包”功能：composer 按钮、ContextPackModal、持久化字段与相关样式全部清理。
- 移除“撤销修改”功能：按钮、编辑记录跟踪与相关样式全部清理。

## 0.8.1 - 2026-08-21

- composer 右下角新增“快捷”按钮，弹出深色垂直指令面板（仿飞书助手风格）。
- 内置 5 个通用指令：正式润色、口语润色、一键翻译、代码审查、笔记整理。
- 支持自定义指令：在设置页“快捷指令”节增删改，立即在面板中显示并标“自定义”角标。
- 点击指令自动把当前编辑器选区与指令拼接后发送给 WorkBuddy；无选区时只发指令。
- 面板支持点击外部自动关闭，底部“管理自定义指令…”跳转设置页。

## 0.8.0 - 2026-08-21

- 发送键在工作时变为红色停止按钮，停止后在回答处标记“已停止”。
- 回答正文中出现的 Vault 文件名自动转为可点击链接，直接打开对应文件。
- 新增主题色设置，立即生效并保留，通过 setCssProps 应用 --wb-blue。
- 过滤 CLI 启动期间的 banner 文本，不再误显示为 AI 回复。
- 支持在输入框粘贴或拖入图片，直接随消息发送给模型分析。
- 新增常驻指令（人设）：输入框输入 # 或点击“人设”按钮编辑，每次发送自动拼接。
- 回答支持选取部分内容复制，新增“复制选中”操作。
- 左下角新增模型选择器：自动读取可用模型列表，可手动切换，切换不重启 CLI。

## 0.7.3 - 2026-08-20

- 将默认工作模板调整为适合不同职业和项目场景的通用模板。
- 默认提供会议纪要、内容润色、汇报提纲、案例总结和执行方案。
- 清理公开文档、界面占位文案和测试样例中的行业限定表达。

## 0.7.2 - 2026-08-20

- 在“@ 添加资料”中增加本地文件上传。
- 支持系统多选、同名自动编号和 200MB 单文件保护。
- 文件复制到 `WorkBuddy/Uploads/` 后自动加入当前任务引用。

## 0.7.1 - 2026-08-20

- 精简输入区操作，只保留右对齐的关闭按钮。
- 保存聊天与任务历史移入任务标签右键菜单。

## 0.7.0 - 2026-08-19

- 新增项目资料包与本地关联笔记推荐。
- 新增任务历史全文搜索、回答收藏和完整 Markdown 导出。
- 新增可配置 GitHub Release 检查与一键更新。

## 0.6.0 - 2026-08-19

- 增强 `@` 引用、来源追溯、选区快捷动作和 `/` 工作模板。

## 0.5.0 - 2026-08-19

- 新增任务持久化、关闭/恢复、重命名、选择性保存、懒连接、Diff 与撤销。

## 0.4.0 - 2026-08-18

- 新增最多 5 个相互独立的 WorkBuddy 任务页。

## 0.3.0 - 2026-08-18

- 固定为工作模式并显示选区引用原文。

## 0.2.0 - 2026-08-18

- 新增跨焦点选区捕获和安全写回。

## 0.1.0 - 2026-08-17

- 首个可安装版本：侧边栏聊天、ACP 接入、上下文和权限确认。
